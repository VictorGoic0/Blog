# Tech Context

## Technologies

- **Build:** Python 3. Custom script only (no static generator framework).
- **Content:** Markdown; YAML frontmatter via `python-frontmatter`.
- **Templating:** Jinja2.
- **Markdown:** `markdown` library (e.g. >=3.5).
- **Styles:** Sass; compile to CSS (e.g. `sass` npm package or equivalent). Output: `css/blog.css`.
- **Hosting:** GitHub Pages. Domain: blog.victorgoico.com (CNAME; DNS at SquareSpace).
- **Analytics:** Google Analytics 4. Measurement ID set in `build.py` (`GA4_MEASUREMENT_ID`); gtag script in base.html when ID is set.

## Development Setup

- Repo root = blog project root. No `blog/` subfolder.
- Python: `requirements.txt` with markdown, python-frontmatter, Jinja2.
- Sass: install compiler (e.g. `sass`); `scss/` → `css/blog.css`.
- Build: `python build.py`; optional `--clean`; optional single-post path.
- CNAME at repo root with content `blog.victorgoico.com`.

## Dependencies (from PRD)

```txt
markdown>=3.5
python-frontmatter>=1.1.0
Jinja2>=3.1.2
```

Sass: add as needed (e.g. npm `sass` or build-step dependency).

## Technical Constraints

- GitHub Pages: static only; one CNAME per repo (hence separate blog repo).
- No server-side code at runtime; no database. All content from Markdown at build time.
- RSS 2.0 and sitemap.xml generated by build script; validate with W3C Feed Validator and sitemap validators.

## Directory Layout (Reference)

- `build.py`, `requirements.txt`, `CNAME`, `README.md`, `.gitignore`, `package.json`, `package-lock.json` at root.
- `posts/`, `templates/`, `scss/`, `css/`, `output/` at root. `designs/` for design reference. `.github/workflows/deploy.yml` for CI/CD. `old-docs/` for archived PRD.
